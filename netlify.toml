[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any unknown path so client routing and
# deep links work. The service worker still handles offline once installed.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
